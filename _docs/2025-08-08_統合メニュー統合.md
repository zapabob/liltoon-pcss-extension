# 統合メニュー導入（lilToon PCSS）

- 日付: 2025-08-08
- 概要: 煩雑なメニューを「統合メニュー」に集約。おすすめ/ライト/マテリアル/VRChat/最適化/エクスポートの6カテゴリ＋検索で、製品運用向けにスリム化。VRChat向けライト制御（トグル＋連続調整）も表情メニューに自動追加できるようにした。

## 変更点（実装）

- 追加: `Editor/UnifiedMenuSystem.cs`
  - [MenuItem] Tools/lilToon PCSS/統合メニュー
  - タブ: ⭐おすすめ / 🔧マテリアル / 💡ライト / 🧩VRChat / ⚙最適化 / 📦エクスポート
  - 検索ボックスで動的フィルタ
  - エラー時のダイアログとConsole出力

- 追加: `Editor/VRChatLightMenuBuilder.cs`
  - [MenuItem] Tools/lilToon PCSS/VRChat/ライト制御メニューを作成
  - VRCExpressionParameters に `PCSS_LightOn(bool)`, `PCSS_LightIntensity(float)` を自動生成
  - VRCExpressionsMenu にサブメニュー「PCSS Light」を作成（Toggle+Radial）
  - 選択中アバターの `VRCAvatarDescriptor` に割当

- 追加: `Editor/VRChatCleanupMenu.cs`
  - [MenuItem] Tools/lilToon PCSS/VRChat向けクリーンアップ/不許可コンポーネント削除
  - 開発用/不許可想定の成分（`PhysBoneLightController`、`lilToon.PCSS.Runtime.ModularAvatarPCSSController` など）と自動配置ライト塊を一括削除

- 既存機能連携（呼び出し統合）
  - 外部ライト自動配置: `Editor/AutoLightPlacement.cs`
  - マテリアルアップグレード: `Editor/LilToonPCSSMaterialUpgrader.cs` → `UpgradeMaterials()` を公開利用
  - パフォーマンス最適化: `Editor/PerformanceOptimizerMenu.cs` → `SetupPerformanceTuner()` を公開利用
  - パッケージ出力: `Editor/PackageExporter.cs` → `ExportPackage()`
  - PhysBoneエミッシブライト: `Editor/PhysBoneEmissiveLightSetup.cs`

## 使い方（要点）

1. 統合メニューを開く
   - メニュー: Tools → lilToon PCSS → 統合メニュー
2. よく使う流れ
   - ⭐おすすめ → 「外部ライト自動配置」→ アバター確認
   - 🧩VRChat → 「ライト制御メニューを作成」→ ToggleでON/OFF、Radialで光量
   - 💡ライト → 「PhysBoneエミッシブライト」→ AutoFix互換の見た目ライト
   - 🧩VRChat → 「クリーンアップ」→ アップロード前に不許可成分を除去

## 製品向け配慮

- 余計なメニューは `#if PCSS_DEV` で開発時のみ露出
- VRChat AutoFixを踏まえ、実ライト制御は避け、エミッシブ主体で連続調整
- クリーニング項目をワンボタン化

## 既知/注意

- VRChat SDKが無い環境では「ライト制御メニューを作成」は案内ダイアログのみ
- 表情メニューは既存構成を上書きしないが、同名パラメータ・サブメニューがある場合は置換

## 参考（設計思考）

- 仮説: メニューは「開始地点の少なさ」と「検索の速さ」で体験が決まる
- 検証: おすすめ最小セット＋検索で到達時間を短縮、VRChat特有のAutoFix制約を事前吸収

---
このログは自動生成（実装完了時）

