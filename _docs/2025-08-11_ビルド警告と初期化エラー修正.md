## 2025-08-11 ビルド警告と初期化エラー修正（実装ログ）

### 対応予定
- CS0414 未使用フィールドの解消（削除 or 使用 or 局所抑制）
- 重複 MenuItem 名の衝突解消（メニュー名の一意化/開発用は `#if PCSS_DEV`）
- lilToon 設定 JSON 空ドキュメント時の初期化保護（空文字時は既定値を充当）
- VRChat SDK バリデーション IndexOutOfRange の再現個所切り分け（Descriptor/Animator の不整合修正）

### メモ
- `lilToonSetting.LoadShaderSetting` で空 JSON が来ると例外、空時は新規設定に差し替える必要あり
- 既存の「VRChat向けクリーンアップ」や「Add Poiyomi PCSS Integration」はメニュー重複のため名称変更か統合メニューに集約する


